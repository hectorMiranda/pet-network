
<tr>
  <td><%= link_to pet.name, user_pet_path(pet) %></td>
  <td></td>
  <td><%= link_to 'Edit', edit_user_pet_path(pet), class: 'btn btn-sm btn-warning' %></td>
  <td><%= link_to 'Remove', user_pet_path(pet), method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-sm btn-danger' %></td>
</tr>



